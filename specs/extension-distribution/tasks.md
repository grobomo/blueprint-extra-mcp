# Spec 012 Tasks

- [x] T003a: Create `scripts/package-extension.js` — build, zip, validate, output to `releases/`
- [x] T003b: Add `releases/` to `.gitignore`
- [x] T003c: Add test script `scripts/test/test-package-extension.sh` — runs package, validates ZIP contents
- [x] T003d: Verify end-to-end: package → unzip → Chrome validates the unpacked extension
