# Spec 013: Post-Merge Verification

## Goal
Run full test suite after PRs #31-34 merged to main. Fix any regressions.

## Scope
- All unit tests (activityTracker, activityReporter, v1Enrichment, statefulBackend, unifiedBackend, extensionServer, oauth, validation)
- Integration tests (MCP process)
- Extension load validation
- Package extension test
